# CLAUDE.md

Commands this repo documents:

```bash
npm run dev
npm run start
npm run lint
```

`npm run start` is declared by `backend/` only, not by the root package —
it must not be reported as stale. `npm run lint` is declared by neither.
