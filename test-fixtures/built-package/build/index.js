// Built output named by package.json "main". It sits in an ignored directory
// but is the package's declared entry point, so it must still be reported.
// (`build/` rather than `dist/`, only because this repo gitignores dist/.)
module.exports = {};
